import { TenantBaseAppEntity, UtilService } from 'kt-nestjs-base';
import { Column, Entity } from 'typeorm';
@UtilService.getEntity
@Entity()
export class __module__(pascalCase) extends TenantBaseAppEntity {
  @Column()
  @UtilService.getProperty
  column_name: string;
}
